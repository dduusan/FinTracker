========================================================================
  FINTRACKER — LOG IMPLEMENTACIJE (Backend Faza 1-8 + Frontend Faza 1-9)
========================================================================

REDOSLED KREIRANJA I OBJAŠNJENJA SVAKOG FAJLA:

========================================================================
1. STRUKTURA FOLDERA
========================================================================

backend/
├── src/
│   ├── config/          → Konfiguracija aplikacije (baza, env varijable, modeli)
│   ├── middleware/       → Middleware funkcije (error handling, auth u budućnosti)
│   ├── modules/          → Poslovni moduli (svaki ima router, service, schemas)
│   │   ├── auth/         → [Faza 3] Autentifikacija
│   │   ├── transactions/ → CRUD za transakcije (prihodi/rashodi)
│   │   ├── categories/   → [Faza 4] CRUD za kategorije transakcija
│   │   ├── budgets/      → [Faza 4] Budžeti po kategorijama sa summary
│   │   └── dashboard/    → [Faza 5] Statistika, trendovi, izveštaji
│   └── utils/            → Pomoćne funkcije (greške, logger)
├── migrations/           → Alembic migracije baze podataka
├── tests/
│   ├── unit/             → Unit testovi
│   └── integration/      → Integracioni testovi
└── frontend/             → [Kasnije] React + Next.js

========================================================================
2. KONFIGURACIONI FAJLOVI (root)
========================================================================

requirements.txt
  → Lista svih Python paketa koji se instaliraju sa "pip install -r requirements.txt"
  → Sadrži: fastapi, uvicorn, sqlalchemy, asyncpg, alembic, pydantic, itd.

.env / .env.example
  → Environment varijable (DATABASE_URL, APP_PORT, JWT_SECRET...)
  → .env se NE COMMITUJE u git (u .gitignore je)
  → .env.example služi kao template za druge developere

.gitignore
  → Govori git-u koje fajlove da ignoriše (.env, __pycache__, venv...)

========================================================================
3. src/config/ — KONFIGURACIJA
========================================================================

settings.py
  → Učitava environment varijable iz .env fajla koristeći pydantic-settings
  → Klasa Settings definiše sve env varijable sa default vrednostima
  → Exportuje "settings" objekat koji se koristi svuda u aplikaciji
  → Primer: settings.DATABASE_URL, settings.APP_PORT

database.py
  → Kreira SQLAlchemy async engine (konekcija ka PostgreSQL bazi)
  → Definiše async_session — factory za kreiranje DB sesija
  → Base klasa — svi SQLAlchemy modeli nasleđuju ovu klasu
  → get_db() — FastAPI dependency koji daje DB sesiju po request-u
    (otvara sesiju na početku requesta, zatvara na kraju)

models.py
  → Definiše SVE tabele u bazi podataka kao Python klase:
    - User: korisnici (id, email, password, name, created_at)
    - Category: kategorije transakcija (id, user_id, name, type, icon)
    - Transaction: transakcije (id, user_id, category_id, amount, type, description, date)
    - Budget: budžeti po kategoriji i mesecu (id, user_id, category_id, amount, month)
  → TransactionType enum: "income" ili "expense"
  → relationship() definiše veze između tabela (npr. User ima mnogo Transaction-a)

========================================================================
4. src/utils/ — POMOĆNE FUNKCIJE
========================================================================

errors.py
  → Custom klase za greške koje aplikacija može baciti:
    - AppError: bazna klasa (message + status_code)
    - NotFoundError(404): resurs nije pronađen
    - ValidationError(400): nevalidan input
    - AuthError(401): neautorizovan pristup
  → Error handler u middleware-u hvata ove greške i vraća JSON odgovor

logger.py
  → Konfiguriše Python logging sa formatiranim ispisom u konzolu
  → U development modu loguje DEBUG nivo, inače INFO
  → Format: "2026-02-10 15:30:00 | INFO     | fintracker | poruka"
  → Exportuje "logger" objekat koji se koristi svuda

========================================================================
5. src/modules/transactions/ — TRANSACTIONS MODUL
========================================================================

schemas.py (Pydantic šeme — VALIDACIJA)
  → Definiše oblik podataka koji ulaze i izlaze iz API-ja:
    - TransactionCreate: šta klijent šalje kad pravi transakciju
      (amount > 0, type, category_id, description, date)
    - TransactionUpdate: šta klijent može da izmeni (sva polja opciona)
    - TransactionResponse: šta API vraća klijentu
    - TransactionFilters: query parametri za filtriranje liste
      (type, category_id, date_from, date_to, page, per_page)
  → Pydantic automatski validira podatke i vraća greške ako su nevalidni

service.py (BIZNIS LOGIKA — sve DB operacije)
  → Funkcije koje rade sa bazom podataka:
    - create_transaction(): kreira novu transakciju u bazi
    - get_transactions(): lista transakcija sa filterima i paginacijom
    - get_transaction_by_id(): jedna transakcija (baca NotFoundError ako ne postoji)
    - update_transaction(): menja polja transakcije (samo ona koja su poslata)
    - delete_transaction(): briše transakciju iz baze
  → Svaka funkcija prima db sesiju i user_id (za izolaciju po korisniku)

router.py (API ENDPOINTI — HTTP rute)
  → Definiše URL putanje i povezuje ih sa service funkcijama:
    POST   /api/transactions/          → kreiranje
    GET    /api/transactions/          → lista (sa filtrima i paginacijom)
    GET    /api/transactions/{id}      → jedna transakcija
    PUT    /api/transactions/{id}      → izmena
    DELETE /api/transactions/{id}      → brisanje
  → Depends(get_current_user): izvlači korisnika iz JWT tokena (Faza 3)
  → Depends(get_db): FastAPI automatski otvara/zatvara DB sesiju

========================================================================
6. src/middleware/ — MIDDLEWARE
========================================================================

auth.py (FAZA 3 — JWT auth middleware)
  → get_current_user(): FastAPI dependency koji:
    1. Čita Authorization header (Bearer <token>)
    2. Dekodira JWT token i izvlači user_id
    3. Učitava korisnika iz baze
    4. Vraća User objekat koji se koristi u endpoint-ima
  → Svaki zaštićeni endpoint dodaje: current_user: User = Depends(get_current_user)

error_handler.py
  → Dva handler-a koje FastAPI poziva kad se desi greška:
    - app_error_handler: hvata naše custom greške (AppError, NotFoundError...)
      i vraća JSON sa odgovarajućim status kodom
    - generic_error_handler: hvata sve ostale neočekivane greške
      i vraća generički 500 Internal Server Error
  → Loguje svaku grešku sa URL-om i metodom

========================================================================
7. src/modules/auth/ — AUTH MODUL (Faza 3)
========================================================================

schemas.py (Pydantic šeme za auth)
  → RegisterRequest: email (5-255 char), password (6-128 char), name (opciono)
  → LoginRequest: email + password
  → TokenResponse: access_token + refresh_token + token_type
  → UserResponse: id, email, name (šta API vraća o korisniku)

service.py (Auth biznis logika)
  → Password:
    - hash_password(): hešira lozinku sa bcrypt (nikad ne čuvamo plaintext!)
    - verify_password(): poredi plaintext sa hešom
  → JWT tokeni:
    - create_access_token(): kratak token (30 min) za pristup API-ju
    - create_refresh_token(): dugačak token (7 dana) za obnovu access tokena
    - decode_token(): dekodira token, proverava tip i istek
  → User operacije:
    - register_user(): kreira novog korisnika (proverava da email nije zauzet)
    - authenticate_user(): proverava email + password, vraća usera
    - get_user_by_id(): učitava usera po ID-u

router.py (Auth API endpointi)
  → POST /api/auth/register  → registracija (vraća UserResponse)
  → POST /api/auth/login     → login (vraća access + refresh token)
  → POST /api/auth/refresh   → novi access token pomoću refresh tokena
  → GET  /api/auth/me        → podaci o trenutnom korisniku (zahteva token)

========================================================================
8. seed.py — TEST PODACI
========================================================================

  → Skripta koja ubacuje test korisnika i kategorije u bazu:
    - User: test@test.com / test123
    - 6 kategorija: Plata, Freelance, Hrana, Transport, Stanarina, Zabava
  → Pokreće se sa: python seed.py

========================================================================
9. src/app.py — ULAZNA TAČKA APLIKACIJE
========================================================================

  → Glavni fajl koji pokreće FastAPI server
  → lifespan(): izvršava se pri pokretanju (kreira tabele) i gašenju servera
  → Registruje error handler-e i router-e (auth, transactions, categories, budgets, dashboard)
  → /api/health endpoint za proveru da li server radi
  → Pokreće uvicorn sa auto-reload u development modu

========================================================================
10. src/modules/categories/ — CATEGORIES MODUL (Faza 4)
========================================================================

schemas.py (Pydantic šeme za kategorije)
  → CategoryCreate: name (1-100 char), type (income/expense), icon (opciono)
  → CategoryUpdate: sva polja opciona
  → CategoryResponse: id, name, type, icon

service.py (Categories biznis logika)
  → create_category(): kreira kategoriju (proverava da ne postoji duplikat)
  → get_categories(): lista kategorija sa opcionalnim filterom po tipu
  → get_category_by_id(): jedna kategorija (baca NotFoundError)
  → update_category(): menja polja kategorije
  → delete_category(): briše kategoriju
  → Svaka funkcija izoluje podatke po user_id

router.py (Categories API endpointi)
  → POST   /api/categories/          → kreiranje nove kategorije
  → GET    /api/categories/          → lista (filter po type: income/expense)
  → GET    /api/categories/{id}      → jedna kategorija
  → PUT    /api/categories/{id}      → izmena
  → DELETE /api/categories/{id}      → brisanje
  → Svi endpointi zahtevaju JWT token (get_current_user)

========================================================================
11. src/modules/budgets/ — BUDGETS MODUL (Faza 4)
========================================================================

schemas.py (Pydantic šeme za budžete)
  → BudgetCreate: category_id, amount (>0), month (prvi dan meseca)
  → BudgetUpdate: amount i month opcioni
  → BudgetResponse: id, category_id, amount, month
  → BudgetSummaryItem: category_name, budgeted, spent, remaining, month
  → BudgetFilters: month, category_id (opcioni filteri)

service.py (Budgets biznis logika)
  → create_budget(): kreira budžet (proverava kategoriju i duplikat)
  → get_budgets(): lista budžeta sa filterima (mesec, kategorija)
  → get_budget_by_id(): jedan budžet (baca NotFoundError)
  → update_budget(): menja iznos ili mesec
  → delete_budget(): briše budžet
  → get_budget_summary(): SPECIJALNA FUNKCIJA
    - Za dati mesec vraća sve budžete sa potrosenim iznosom
    - Računa "spent" iz tabele Transaction (expense tip, isti mesec)
    - Vraća: budgeted, spent, remaining za svaku kategoriju

router.py (Budgets API endpointi)
  → POST   /api/budgets/             → kreiranje novog budžeta
  → GET    /api/budgets/             → lista (filter po month, category_id)
  → GET    /api/budgets/summary?month=2026-02-01 → pregled potrošnje
  → GET    /api/budgets/{id}         → jedan budžet
  → PUT    /api/budgets/{id}         → izmena
  → DELETE /api/budgets/{id}         → brisanje
  → Svi endpointi zahtevaju JWT token

========================================================================
12. src/modules/dashboard/ — DASHBOARD MODUL (Faza 5)
========================================================================

schemas.py (Pydantic šeme za statistiku)
  → SummaryResponse: total_income, total_expense, balance, transaction_count
  → MonthlyItem: month ("2026-02"), income, expense, balance
  → MonthlyResponse: lista mesečnih stavki + months_count
  → CategorySpending: category_name, total, percentage, transaction_count
  → ByCategoryResponse: lista kategorija + grand_total
  → RecentTransaction: amount, type, description, date, category_name, icon

service.py (Dashboard biznis logika — AGREGACIONI UPITI)
  → get_summary(): ukupni prihodi, rashodi i bilans za dati period
    - Koristi SQL CASE + SUM za jednu efikasnu pretragu
    - Opcioni filteri: date_from, date_to
  → get_monthly_trends(): mesečni trend za poslednjih N meseci
    - GROUP BY godina + mesec
    - Vraća income vs expense za svaki mesec
  → get_by_category(): potrošnja/prihod po kategorijama
    - JOIN Transaction + Category
    - Računa procenat svake kategorije od ukupnog iznosa
    - Sortirano po iznosu (najveći prvi)
  → get_recent_transactions(): poslednjih N transakcija
    - JOIN sa Category za ime i ikonu kategorije
    - Sortirano po datumu (najnovije prvo)

router.py (Dashboard API endpointi)
  → GET /api/dashboard/summary?date_from=...&date_to=...
    → ukupni prihodi, rashodi, bilans za period
  → GET /api/dashboard/monthly?months=6
    → mesečni trend (default 6 meseci unazad)
  → GET /api/dashboard/by-category?type=expense&date_from=...&date_to=...
    → potrošnja po kategorijama sa procentima
  → GET /api/dashboard/recent?limit=10
    → poslednjih N transakcija sa kategorijom
  → Svi endpointi zahtevaju JWT token

========================================================================
13. src/config/redis.py — REDIS KONEKCIJA (Faza 6)
========================================================================

  → init_redis(): kreira async Redis klijent pri pokretanju aplikacije
  → close_redis(): zatvara konekciju pri gašenju
  → get_redis(): vraća Redis klijent (koristi se u cache i rate limiter)
  → Koristi redis[hiredis] paket za optimalne performanse

========================================================================
14. src/utils/cache.py — REDIS CACHING (Faza 6)
========================================================================

  → cache_get(key): čita keširanu vrednost (JSON deserijalizacija)
  → cache_set(key, value, ttl): upisuje u keš (default 5 min TTL)
  → cache_delete(pattern): briše ključeve po patternu (wildcard *)
  → invalidate_user_dashboard(user_id): briše SVE dashboard keš ključeve
    za korisnika — poziva se kad se menjaju transakcije/budžeti/kategorije
  → Prefiks "fintracker:" za sve ključeve (izolacija od drugih aplikacija)

========================================================================
15. src/middleware/rate_limiter.py — RATE LIMITING (Faza 6)
========================================================================

  → RateLimitMiddleware: Starlette middleware koji ograničava zahteve
  → Dva profila:
    - auth rute: 10 zahteva/min (zaštita od brute-force napada)
    - ostale rute: 60 zahteva/min
  → Identifikuje klijenta po IP adresi
  → Redis INCR + EXPIRE za atomično brojanje zahteva
  → Response headers: X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  → HTTP 429 Too Many Requests sa Retry-After headerom
  → Graceful degradation: ako Redis nije dostupan, propušta zahteve

========================================================================
16. CACHE INVALIDACIJA (Faza 6)
========================================================================

  → Kad korisnik kreira/menja/briše transakciju, budžet ili kategoriju,
    automatski se briše dashboard keš za tog korisnika
  → Ažurirani routeri: transactions, budgets, categories
  → Dashboard endpointi: proveravaju keš pre upita u bazu
    - Cache HIT → odmah vraća rezultat (bez DB upita)
    - Cache MISS → upit u bazu, rezultat se keršira za 5 min

========================================================================
17. tests/ — TESTOVI (Faza 7)
========================================================================

  Tehnologija: pytest + pytest-asyncio + httpx + aiosqlite

  conftest.py (Test konfiguracija)
  → SQLite in-memory baza (NE TREBA Docker za testove!)
  → Mock Redis (testovi rade bez Redis servera)
  → Fixture-i:
    - setup_database: kreira/briše tabele pre/posle svakog testa
    - db: test DB sesija
    - client: httpx AsyncClient koji komunicira sa FastAPI app-om
    - test_user: kreira test korisnika (test@test.com / test123)
    - auth_headers: Authorization header sa validnim JWT tokenom
    - test_categories: 3 test kategorije (Plata, Hrana, Transport)

  test_auth.py (10 testova)
  → Registracija: uspešna, dupli email, nevalidan email, kratka šifra
  → Login: uspešan, pogrešna šifra, nepostojeći korisnik
  → Refresh: validan token, nevalidan token
  → Me: sa tokenom, bez tokena

  test_transactions.py (8 testova)
  → Create: uspešno, bez auth-a, negativan iznos
  → List: prazna lista, sa podacima, filter po tipu
  → Get: po ID-u, nepostojeći
  → Update: izmena iznosa i opisa
  → Delete: brisanje + provera da ne postoji

  test_categories.py (8 testova)
  → Create: uspešno, duplikat, bez auth-a
  → List: sve, filter po tipu, prazna lista
  → Get: po ID-u, nepostojeći
  → Update: izmena imena i ikone
  → Delete: brisanje + provera

  test_budgets.py (8 testova)
  → Create: uspešno, duplikat (ista kategorija+mesec), nepostojeća kategorija
  → List: svi, filter po mesecu
  → Summary: sa potrošnjom (budgeted vs spent vs remaining), prazan mesec
  → Update: izmena iznosa
  → Delete: brisanje + provera

  test_dashboard.py (8 testova)
  → Summary: prazan, sa podacima, sa date filterom, bez auth-a
  → Monthly: mesečni trend
  → By-category: expense kategorije, income kategorije
  → Recent: poslednjih N transakcija, prazna lista

  pytest.ini
  → asyncio_mode = auto (automatski async testovi)
  → testpaths = tests

========================================================================
18. Dockerfile — DOCKER IMAGE (Faza 8)
========================================================================

  → Python 3.12-slim bazni image (mali footprint)
  → Instalira requirements.txt
  → Kopira source kod
  → Pokreće uvicorn na portu 8000
  → .dockerignore: isključuje venv/, tests/, __pycache__/, .env

========================================================================
19. docker-compose.yml — DOCKER COMPOSE (Faza 8)
========================================================================

  → 3 servisa:
    - app: FastAPI aplikacija (build iz Dockerfile-a)
    - postgres: PostgreSQL 16
    - redis: Redis 7-alpine
  → Healthcheck-ovi: app čeka da postgres i redis budu healthy
  → Environment: DATABASE_URL i REDIS_URL koriste Docker DNS (postgres, redis)
  → JWT_SECRET se čita iz environment varijable ili .env
  → restart: unless-stopped za sve servise

========================================================================
20. .github/workflows/ci.yml — CI/CD (Faza 8)
========================================================================

  → Pokreće se na push/PR ka main grani
  → 3 posla (jobs):
    1. test: instalira dependencies, pokreće pytest
    2. lint: proverava da se app importuje bez greške
    3. docker: build-uje Docker image (samo na main grani, nakon test+lint)
  → Testovi koriste SQLite in-memory (ne trebaju Docker servise)

========================================================================
POKRETANJE:
========================================================================

  1. python -m venv venv              → kreira virtualno okruženje
  2. venv\Scripts\activate            → aktivira ga (Windows)
  3. pip install -r requirements.txt  → instalira pakete
  4. docker compose up -d             → pokreće PostgreSQL + Redis u Dockeru
  5. python seed.py                   → ubacuje test korisnika i kategorije
  6. python -m uvicorn src.app:app --reload --port 8000  → pokreće server
  7. Otvori http://localhost:8000/docs → Swagger UI za testiranje API-ja

========================================================================
TESTIRANJE AUTH FLOW-a:
========================================================================

  1. POST /api/auth/register → kreiraj nalog (ili koristi seed korisnika)
  2. POST /api/auth/login    → email: test@test.com, password: test123
     → dobijaš access_token i refresh_token
  3. Kopiraj access_token i dodaj ga u Swagger "Authorize" dugme:
     Bearer <access_token>
  4. Sad možeš koristiti sve zaštićene endpointe
  5. GET /api/auth/me → proverava ko si
  6. POST /api/auth/refresh → kad access_token istekne (30 min)

========================================================================
TESTIRANJE CATEGORIES + BUDGETS (Faza 4):
========================================================================

  (Prethodno se uloguj i dodaj Bearer token u Swagger)

  CATEGORIES:
  1. GET  /api/categories/          → lista kategorija (seed je napravio 6)
  2. POST /api/categories/          → nova kategorija:
     {"name": "Investicije", "type": "income", "icon": "📈"}
  3. GET  /api/categories/?type=expense → samo expense kategorije
  4. PUT  /api/categories/{id}      → izmeni kategoriju
  5. DELETE /api/categories/{id}    → obriši kategoriju

  BUDGETS:
  1. POST /api/budgets/             → novi budžet:
     {"category_id": 3, "amount": 15000, "month": "2026-02-01"}
  2. GET  /api/budgets/             → lista svih budžeta
  3. GET  /api/budgets/?month=2026-02-01 → budžeti za februar
  4. GET  /api/budgets/summary?month=2026-02-01
     → pregled: koliko je budžetirano vs potrošeno za svaku kategoriju
  5. PUT  /api/budgets/{id}         → izmeni iznos budžeta
  6. DELETE /api/budgets/{id}       → obriši budžet

========================================================================
TESTIRANJE DASHBOARD (Faza 5):
========================================================================

  (Prethodno se uloguj, dodaj Bearer token, i napravi par transakcija)

  1. GET /api/dashboard/summary
     → ukupni prihodi, rashodi, bilans (svih transakcija)
  2. GET /api/dashboard/summary?date_from=2026-02-01&date_to=2026-02-28
     → samo za februar
  3. GET /api/dashboard/monthly?months=6
     → mesečni trend za poslednjih 6 meseci
  4. GET /api/dashboard/by-category?type=expense
     → koliko si potrošio po kategorijama (sa procentima)
  5. GET /api/dashboard/by-category?type=income
     → koliko si zaradio po kategorijama
  6. GET /api/dashboard/recent?limit=5
     → poslednjih 5 transakcija sa imenom kategorije

========================================================================
KOMANDE ZA POKRETANJE I ZAUSTAVLJANJE:
========================================================================

  POKRETANJE (redosled je bitan):
  1. Otvori Docker Desktop              → sačekaj da se pokrene (zelena ikonica)
  2. cd backend                          → uđi u backend folder
  3. venv\Scripts\activate               → aktiviraj virtualno okruženje
  4. docker compose up -d                → pokreni PostgreSQL + Redis kontejnere
  5. pip install -r requirements.txt     → instaliraj pakete (samo ako ima promena)
  6. python seed.py                      → ubaci test podatke (samo prvi put)
  7. python -m uvicorn src.app:app --reload --port 8000  → pokreni server
  8. Otvori http://localhost:8000/docs   → Swagger UI za testiranje

  ZAUSTAVLJANJE (obrnut redosled):
  1. Ctrl + C                            → zaustavi server (u terminalu gde radi)
  2. docker compose down                 → zaustavi PostgreSQL + Redis kontejnere
  3. deactivate                          → deaktiviraj venv (opciono)

  ČIST RESTART (briše sve podatke iz baze):
  1. Ctrl + C                            → zaustavi server
  2. docker compose down -v              → zaustavi kontejnere i obriši bazu
  3. docker compose up -d                → ponovo pokreni kontejnere
  4. python seed.py                      → ponovo ubaci test podatke
  5. python -m uvicorn src.app:app --reload --port 8000  → pokreni server

========================================================================
========================================================================
  FRONTEND — FAZA 1: Projekat setup + osnovni layout
========================================================================
========================================================================

========================================================================
21. frontend/package.json — Zavisnosti (Frontend Faza 1)
========================================================================

  → Kreiran sa: npm create vite@latest frontend -- --template react-ts
  → Glavne zavisnosti:
    - react, react-dom (v19)    → UI framework
    - react-router-dom          → klijentski routing (SPA navigacija)
    - axios                     → HTTP klijent za API pozive
    - recharts                  → grafikoni (bar, line, pie chart)
    - react-hook-form           → upravljanje formama
    - react-hot-toast           → toast notifikacije (uspeh/greška)
    - lucide-react              → SVG ikone
    - date-fns                  → formatiranje datuma
  → Dev zavisnosti:
    - tailwindcss + @tailwindcss/vite → CSS framework
    - typescript                → type safety

========================================================================
22. frontend/vite.config.ts — Vite konfiguracija (Frontend Faza 1)
========================================================================

  → Tailwind CSS plugin: automatski procesira Tailwind klase
  → Dev server na portu 3000 (frontend)
  → API proxy: /api/* zahtevi se prosleđuju na http://localhost:8000 (backend)
    - Ovo znači da u kodu pišeš axios.get("/api/health")
      a Vite to šalje na backend automatski
    - Nema CORS problema u razvoju jer proxy radi kao posrednik

========================================================================
23. frontend/src/lib/utils.ts — Helper funkcije (Frontend Faza 1)
========================================================================

  → formatCurrency(amount) → formatira broj u RSD valuti (srpski format)
  → formatDate(date)       → formatira datum na srpskom (dd. MMM yyyy.)
  → formatMonth(date)      → formatira mesec na srpskom (januar 2026.)
  → cn(...classes)          → spaja CSS klase (filtrira false/null/undefined)

========================================================================
24. frontend/src/components/ui/ — Reusable komponente (Frontend Faza 1)
========================================================================

  Button.tsx:
  → Univerzalno dugme sa 4 varijante (primary, secondary, danger, ghost)
  → 3 veličine (sm, md, lg)
  → Loading spinner (prikazuje se dok se čeka odgovor sa servera)
  → Automatski disabled dok je loading

  Input.tsx:
  → Input polje sa labelom i error porukom
  → Koristi forwardRef (kompatibilno sa react-hook-form)
  → Vizuelni feedback za grešku (crvena granica)

  Select.tsx:
  → Dropdown sa opcijama, labelom i error porukom
  → Ista struktura kao Input (label, error, forwardRef)

  Card.tsx:
  → Kontejner sa belom pozadinom, zaobljenim uglovima i senkom
  → Opcioni naslov (title prop)

  Modal.tsx:
  → Overlay (zatamnjeni pozadinski sloj, klik zatvara modal)
  → Centriran beli prozor sa naslovom i X dugmetom
  → Blokira scroll dok je otvoren

========================================================================
25. frontend/src/layouts/DashboardLayout.tsx — Layout (Frontend Faza 1)
========================================================================

  → Bočni meni (sidebar) sa 4 navigacione stavke:
    - Pregled (/)         → LayoutDashboard ikona
    - Transakcije (/transactions) → ArrowLeftRight ikona
    - Kategorije (/categories)    → Tag ikona
    - Budžeti (/budgets)          → Wallet ikona
  → "Odjavi se" dugme na dnu sidebar-a
  → Header sa hamburger menijem (na mobilnom)
  → Responsivan: sidebar se sakriva na ekranima < 1024px
  → NavLink: aktivna stavka je obojena indigo bojom
  → <Outlet /> renderuje trenutnu stranicu

========================================================================
26. frontend/src/pages/ — Stranice (Frontend Faza 1)
========================================================================

  → DashboardPage.tsx     → placeholder za Fazu 3
  → TransactionsPage.tsx  → placeholder za Fazu 4
  → CategoriesPage.tsx    → placeholder za Fazu 5
  → BudgetsPage.tsx       → placeholder za Fazu 6
  → LoginPage.tsx         → placeholder za Fazu 2
  → RegisterPage.tsx      → placeholder za Fazu 2

========================================================================
27. frontend/src/App.tsx — Routing (Frontend Faza 1)
========================================================================

  → BrowserRouter: klijentski routing (URL se menja bez reload-a)
  → Toaster: globalne toast notifikacije (gore desno)
  → Auth rute (/login, /register): bez layout-a (pun ekran)
  → Dashboard rute: omotane u DashboardLayout (sidebar + header)
    - /              → DashboardPage
    - /transactions  → TransactionsPage
    - /categories    → CategoriesPage
    - /budgets       → BudgetsPage

========================================================================
28. backend/src/app.py — CORS middleware (Frontend Faza 1)
========================================================================

  → Dodat CORSMiddleware koji dozvoljava zahteve sa http://localhost:3000
  → allow_credentials=True: dozvoljava slanje kolačića
  → allow_methods=["*"]: svi HTTP metodi (GET, POST, PUT, DELETE)
  → allow_headers=["*"]: svi header-i (uključujući Authorization)
  → Ovo je potrebno jer frontend (port 3000) i backend (port 8000)
    su na različitim portovima = "cross-origin" zahtevi

========================================================================
========================================================================
  FRONTEND — FAZA 2: Autentifikacija (Login/Register)
========================================================================
========================================================================

========================================================================
29. frontend/src/api/axios.ts — Axios instanca (Frontend Faza 2)
========================================================================

  → Kreira axios instancu sa baseURL: "/api"
  → Request interceptor: automatski dodaje Authorization: Bearer <token>
    na svaki zahtev (čita token iz localStorage)
  → Response interceptor: ako server vrati 401 (Unauthorized):
    1. Pokušava da osvezi token preko POST /api/auth/refresh
    2. Ako uspe → sačuva nove tokene i ponovi originalni zahtev
    3. Ako ne uspe → obriše tokene i redirectuje na /login
  → Ne pokušava refresh za /auth/login i /auth/refresh rute (izbegava loop)
  → _retry flag sprečava beskonačno ponavljanje

========================================================================
30. frontend/src/api/auth.ts — Auth API pozivi (Frontend Faza 2)
========================================================================

  → TypeScript interfejsi: User, TokenResponse, LoginRequest, RegisterRequest
  → login(data)    → POST /api/auth/login → vraća access + refresh token
  → register(data) → POST /api/auth/register → vraća kreiranog korisnika
  → getMe()        → GET /api/auth/me → vraća podatke o ulogovanom korisniku

========================================================================
31. frontend/src/context/AuthContext.tsx — Auth state (Frontend Faza 2)
========================================================================

  → React Context koji drži globalni auth state (user, loading)
  → AuthProvider omotava celu aplikaciju
  → useAuth() hook — pristup auth stanju iz bilo koje komponente
  → Pri učitavanju stranice: proverava da li postoji token u localStorage,
    ako postoji → poziva getMe() da dohvati korisnika
  → login() funkcija: poziva API, čuva tokene u localStorage, dohvata usera
  → register() funkcija: poziva API (ne loguje automatski)
  → logout() funkcija: briše tokene iz localStorage, resetuje user na null

========================================================================
32. frontend/src/components/ProtectedRoute.tsx — Zaštita ruta (Frontend Faza 2)
========================================================================

  → Wrapper komponenta koja štiti rute od neautorizovanih korisnika
  → Dok se proverava auth status → prikazuje loading spinner
  → Ako korisnik nije ulogovan → redirectuje na /login
  → Ako je ulogovan → renderuje children (stranica koju korisnik traži)

========================================================================
33. frontend/src/pages/LoginPage.tsx — Login stranica (Frontend Faza 2)
========================================================================

  → Forma sa email i lozinka poljima (react-hook-form)
  → Validacija: email obavezan + format, lozinka min 6 karaktera
  → Na uspeh: toast "Uspešna prijava!" + redirect na /
  → Na grešku: toast sa error porukom sa servera
  → Loading state: dugme prikazuje spinner dok čeka odgovor
  → Ako je korisnik već ulogovan → automatski redirect na /
  → Link ka /register stranici

========================================================================
34. frontend/src/pages/RegisterPage.tsx — Registracija (Frontend Faza 2)
========================================================================

  → Forma: ime (opciono), email, lozinka, potvrda lozinke
  → Validacija: email format, lozinka min 6, potvrda mora da se poklapa
  → Na uspeh: toast + redirect na /login (korisnik se ručno prijavljuje)
  → Na grešku: toast sa error porukom (npr. "Email već postoji")
  → Ako je korisnik već ulogovan → automatski redirect na /

========================================================================
35. frontend/src/App.tsx — Ažuriran routing (Frontend Faza 2)
========================================================================

  → AuthProvider omotava sve rute (globalni auth state)
  → Javne rute: /login, /register (bez zaštite)
  → Zaštićene rute: /, /transactions, /categories, /budgets
    → Omotane u ProtectedRoute → redirect na /login ako nema tokena
  → DashboardLayout se renderuje samo za ulogovane korisnike

========================================================================
36. frontend/src/layouts/DashboardLayout.tsx — Ažuriran (Frontend Faza 2)
========================================================================

  → Header prikazuje ime ili email ulogovanog korisnika
  → "Odjavi se" dugme poziva logout() i redirectuje na /login

========================================================================
========================================================================
  FRONTEND — FAZA 3: Dashboard (grafikoni i statistike)
========================================================================
========================================================================

========================================================================
37. frontend/src/api/dashboard.ts — Dashboard API (Frontend Faza 3)
========================================================================

  → TypeScript interfejsi za sve dashboard odgovore:
    - SummaryResponse: ukupni prihodi, rashodi, bilans, broj transakcija
    - MonthlyItem / MonthlyResponse: mesečni trend podaci
    - CategorySpending / ByCategoryResponse: rashodi po kategorijama
    - RecentTransaction: poslednje transakcije sa imenom kategorije
  → getSummary()     → GET /api/dashboard/summary
  → getMonthly()     → GET /api/dashboard/monthly?months=6
  → getByCategory()  → GET /api/dashboard/by-category?type=expense
  → getRecent()      → GET /api/dashboard/recent?limit=8

========================================================================
38. frontend/src/hooks/useDashboard.ts — Custom hook (Frontend Faza 3)
========================================================================

  → Jedan hook koji poziva sva 4 dashboard endpoint-a paralelno (Promise.all)
  → Drži state: summary, monthly, byCategory, grandTotal, recent, loading, error
  → reload() funkcija za osvežavanje podataka
  → Koristi useEffect da automatski učita podatke pri montiranju komponente

========================================================================
39. frontend/src/components/dashboard/SummaryCards.tsx (Frontend Faza 3)
========================================================================

  → 4 kartice u grid-u (responsive: 1 → 2 → 4 kolone)
  → Ukupni prihodi: zelena ikona TrendingUp
  → Ukupni rashodi: crvena ikona TrendingDown
  → Bilans: indigo/crvena boja zavisno od pozitivnog/negativnog bilansa
  → Broj transakcija: siva ikona ArrowLeftRight
  → Koristi formatCurrency() za RSD format

========================================================================
40. frontend/src/components/dashboard/MonthlyChart.tsx (Frontend Faza 3)
========================================================================

  → Grupovani bar chart sa Recharts bibliotekom
  → X osa: skraćeni nazivi meseci na srpskom (Jan, Feb, Mar...)
  → Y osa: vrednosti u hiljadama (10k, 20k...)
  → 2 bара: Prihodi (indigo) i Rashodi (crvena)
  → Tooltip prikazuje tačne iznose u RSD
  → ResponsiveContainer: automatski se prilagođava širini ekrana

========================================================================
41. frontend/src/components/dashboard/CategoryPieChart.tsx (Frontend Faza 3)
========================================================================

  → Donut chart (pie sa innerRadius) sa Recharts bibliotekom
  → Toggle dugme Rashodi/Prihodi (UI spreman, logika u Fazi 6)
  → 8 različitih boja za kategorije (rotira se ako ih ima više)
  → Tooltip sa RSD iznosom
  → Prikazuje ukupan iznos ispod grafikona
  → Empty state ako nema podataka

========================================================================
42. frontend/src/components/dashboard/RecentTransactions.tsx (Frontend Faza 3)
========================================================================

  → Lista poslednjih 8 transakcija
  → Svaka stavka: ikona (zelena strelica gore za prihod, crvena dole za rashod)
  → Prikazuje: ime kategorije sa emoji ikonom, opis/datum, iznos
  → Iznos: zeleni sa "+" za prihode, crveni sa "-" za rashode
  → Empty state ako nema transakcija

========================================================================
43. frontend/src/pages/DashboardPage.tsx — Ažuriran (Frontend Faza 3)
========================================================================

  → Koristi useDashboard hook za dohvatanje svih podataka
  → Loading spinner dok se podaci učitavaju
  → Error poruka ako API poziv ne uspe
  → Layout: summary kartice gore, 2 grafikona u sredini, transakcije dole
  → Grid: na manjim ekranima 1 kolona, na xl 2 kolone za grafikone

========================================================================
========================================================================
  FRONTEND — FAZA 4: Transakcije (CRUD)
========================================================================
========================================================================

========================================================================
44. frontend/src/api/transactions.ts — Transakcije API (Frontend Faza 4)
========================================================================

  → TypeScript interfejsi: Transaction, TransactionCreate, TransactionUpdate, TransactionFilters
  → getTransactions(filters) → GET /api/transactions/ (sa filterima)
    - Filtrira prazne vrednosti pre slanja na server
  → createTransaction(data) → POST /api/transactions/
  → updateTransaction(id, data) → PUT /api/transactions/{id}
  → deleteTransaction(id) → DELETE /api/transactions/{id}

========================================================================
45. frontend/src/api/categories.ts — Kategorije API (Frontend Faza 4)
========================================================================

  → TypeScript interfejsi: Category, CategoryCreate, CategoryUpdate
  → getCategories(type?) → GET /api/categories/ (opcioni filter po tipu)
  → createCategory, updateCategory, deleteCategory
  → Ovaj fajl se koristi u Fazi 4 (transakcije), 5 (kategorije) i 6 (budzeti)

========================================================================
46. frontend/src/components/transactions/TransactionFilters.tsx (Faza 4)
========================================================================

  → Filter traka sa 4 kontrole: tip, kategorija, datum od, datum do
  → "Resetuj" dugme vraca filtere na podrazumevane vrednosti
  → Svaka promena filtera automatski osvezava listu transakcija
  → Dropdown za kategorije prikazuje emoji ikonu i naziv

========================================================================
47. frontend/src/components/transactions/TransactionForm.tsx (Faza 4)
========================================================================

  → Modal forma za kreiranje i izmenu transakcije
  → Tip: radio dugmad (Rashod/Prihod) sa bojama (crvena/zelena)
  → Kategorija dropdown: filtrira se po izabranom tipu
    (ako izaberes "Prihod", vidis samo income kategorije)
  → Polja: iznos, kategorija, datum, opis (opciono)
  → Pri otvaranju za izmenu: forma se popunjava postojecim podacima
  → useEffect resetuje formu svaki put kad se modal otvori

========================================================================
48. frontend/src/components/transactions/DeleteConfirmModal.tsx (Faza 4)
========================================================================

  → Jednostavan modal za potvrdu brisanja
  → Dugmad: "Otkazi" (siva) i "Obrisi" (crvena)
  → Loading state na "Obrisi" dugmetu dok se ceka odgovor

========================================================================
49. frontend/src/components/transactions/TransactionTable.tsx (Faza 4)
========================================================================

  → Tabela sa kolonama: Datum, Kategorija, Opis, Tip, Iznos, Akcije
  → Opis i kategorija su skriveni na malim ekranima (responsive)
  → Tip prikazan kao badge (zeleni za prihod, crveni za rashod)
  → Iznos: zeleni sa "+" za prihode, crveni sa "-" za rashode
  → Dugmad za izmenu (olovka) i brisanje (kanta) na svakom redu
  → Empty state ako nema transakcija

========================================================================
50. frontend/src/pages/TransactionsPage.tsx — Azuriran (Frontend Faza 4)
========================================================================

  → Kompletna CRUD stranica za transakcije
  → State: transactions, categories, filters, loading, formOpen, editTx, deleteTx
  → useCallback za loadTransactions (poziva se kad se filters promene)
  → handleCreate: kreira transakciju, zatvara modal, osvezava listu
  → handleEdit: azurira transakciju, zatvara modal, osvezava listu
  → handleDelete: brise transakciju, zatvara modal, osvezava listu
  → Toast notifikacije za uspeh i greske
  → "Nova transakcija" dugme gore desno

========================================================================
51. backend/seed.py — Azuriran (Frontend Faza 4)
========================================================================

  → Dodate 16 test transakcija za poslednja 3 meseca
  → Mesecna plata, freelance prihodi, stanarina, hrana, transport, zabava
  → Korisno za testiranje dashboard grafikona i filtera

========================================================================
========================================================================
  FRONTEND — FAZA 5: Kategorije (CRUD)
========================================================================
========================================================================

========================================================================
52. frontend/src/components/categories/CategoryForm.tsx (Frontend Faza 5)
========================================================================

  → Modal forma za kreiranje i izmenu kategorije
  → Tip: radio dugmad (Rashod/Prihod) sa bojama (crvena/zelena)
  → Naziv: tekst input sa obaveznom validacijom
  → Ikona: dve opcije:
    - Brzi izbor: 20 unapred ponudenih emoji ikonice (klik selektuje)
    - Slobodan unos: tekst polje za bilo koji emoji ili tekst
  → Selektovana ikona ima indigo border (vizuelni feedback)
  → useEffect resetuje formu svaki put kad se modal otvori
  → Pri izmeni: popunjava se sa postojecim podacima kategorije

========================================================================
53. frontend/src/components/categories/CategoryCard.tsx (Frontend Faza 5)
========================================================================

  → Kartica koja prikazuje jednu kategoriju
  → Levo: emoji ikona u obojenoj pozadini (zelena za prihod, crvena za rashod)
  → Sredina: naziv kategorije + tip badge (Prihod/Rashod)
  → Desno: dugme za izmenu (olovka, hover indigo) i brisanje (kanta, hover crvena)
  → Hover efekat na celoj kartici (border postaje tamniji)
  → Ako kategorija nema ikonu prikazuje se podrazumevano "📂"

========================================================================
54. frontend/src/pages/CategoriesPage.tsx — Azuriran (Frontend Faza 5)
========================================================================

  → Kompletna CRUD stranica za kategorije
  → Podeljena u 2 sekcije (grid, 2 kolone na vecim ekranima):
    - Prihodi (zeleni naslov + broj kategorija)
    - Rashodi (crveni naslov + broj kategorija)
  → "Nova kategorija" dugme gore desno
  → Empty state za svaku sekciju ako nema kategorija
  → State: categories, loading, formOpen, editCat, deleteCat, saving, deleting
  → handleCreate: kreira kategoriju, zatvara modal, osvezava listu
  → handleEdit: azurira kategoriju, zatvara modal, osvezava listu
  → handleDelete: brise kategoriju uz toast notifikaciju
    - Napomena: kategorija se NE MOZE obrisati ako ima transakcije
      (backend baca gresku zbog foreign key constrainta)
      poruka upozorenja je prikazana u modal-u za brisanje
  → Modal za brisanje (inline, ne koristi DeleteConfirmModal komponentu)
    sadrzi naziv kategorije i upozorenje o transakcijama

========================================================================
========================================================================
  FRONTEND — FAZA 6: Budžeti (CRUD + progress barovi)
========================================================================
========================================================================

========================================================================
55. frontend/src/api/budgets.ts — Budžeti API (Frontend Faza 6)
========================================================================

  → TypeScript interfejsi: Budget, BudgetCreate, BudgetUpdate, BudgetSummaryItem
  → getBudgets(month?) → GET /api/budgets/ (opcioni filter po mesecu)
  → getBudgetSummary(month) → GET /api/budgets/summary?month=...
    - Ovo je NAJVAŽNIJI endpoint — vraća budgeted, spent, remaining za svaku kat.
  → createBudget(data) → POST /api/budgets/
  → updateBudget(id, data) → PUT /api/budgets/{id}
  → deleteBudget(id) → DELETE /api/budgets/{id}

========================================================================
56. frontend/src/components/budgets/BudgetForm.tsx (Frontend Faza 6)
========================================================================

  → Modal forma za kreiranje i izmenu budžeta
  → Dropdown za kategoriju: prikazuje samo EXPENSE kategorije
    - Pri kreiranju: filtrira kategorije koje vec imaju budzet za taj mesec
    - Pri izmeni: dropdown je disabled (ne moze se promeniti kategorija)
  → Input za planirani iznos (RSD, tip number, min 1)
  → Automatski koristi trenutni mesec (prosledjen kao prop)
  → useEffect resetuje formu pri otvaranju modala

========================================================================
57. frontend/src/components/budgets/BudgetCard.tsx (Frontend Faza 6)
========================================================================

  → Kartica sa progress barom za jedan budzet
  → Prikazuje: naziv kategorije, potroseno / od ukupnog, procenat
  → Progress bar sa 3 boje:
    - Zelena (< 70%) → sve u redu
    - Žuta (70-89%) → blizu limita
    - Crvena (>= 90%) → kritično
  → Ako je potrošnja VEĆA od budžeta → prikazuje "Prekoračenje: XXX RSD"
  → Ako je manja → prikazuje "Preostalo: XXX RSD" (zelena boja)
  → Dugmad za izmenu (olovka) i brisanje (kanta)

========================================================================
58. frontend/src/pages/BudgetsPage.tsx — Ažuriran (Frontend Faza 6)
========================================================================

  → Kompletna CRUD stranica za budžete sa mesečnom navigacijom
  → Navigacija meseca: strelice levo/desno menjaju mesec
    - Format: "Februar 2026" (srpski nazivi meseci)
  → Koristi /api/budgets/summary za prikaz (budgeted vs spent vs remaining)
  → Ukupna statistika na vrhu:
    - Ukupan progress bar (indigo/žuta/crvena boja)
    - Ukupno potrošeno od ukupno budžetirano
    - Preostalo ili prekoračenje
  → Grid sa BudgetCard karticama (1 / 2 / 3 kolone zavisno od ekrana)
  → Empty state: "Nema budžeta za ovaj mesec" sa uputstvom
  → CRUD: kreiranje, izmena (samo iznos), brisanje sa potvrdom
  → Promise.all za paralelno ucitavanje summary + categories

========================================================================
========================================================================
  FRONTEND — FAZA 7: Polish + UX poboljšanja
========================================================================
========================================================================

========================================================================
59. frontend/src/components/ui/Skeleton.tsx (Frontend Faza 7)
========================================================================

  → Reusable skeleton komponenta za loading state
  → Koristi animate-pulse Tailwind animaciju
  → Prima className za prilagodenu visinu/sirinu

========================================================================
60. frontend/src/components/dashboard/DashboardSkeleton.tsx (Faza 7)
========================================================================

  → Skeleton placeholder za celu dashboard stranicu
  → Imitira raspored: 4 summary kartice, 2 grafikona, lista transakcija
  → Prikazuje se umesto spinnera dok se podaci ucitavaju
  → Mnogo bolji UX jer korisnik vidi obris stranice pre ucitavanja

========================================================================
61. frontend/src/components/ErrorBoundary.tsx (Frontend Faza 7)
========================================================================

  → React Error Boundary (class komponenta — jedini nacin u React-u)
  → Hvata JavaScript greske u bilo kojoj child komponenti
  → Umesto belog ekrana prikazuje:
    - Ikona upozorenja
    - Poruka: "Nešto je pošlo naopako"
    - Dugme "Osveži stranicu" (reload)
  → Loguje gresku u konzolu (console.error)
  → Omotava sve rute u App.tsx

========================================================================
62. frontend/src/hooks/usePageTitle.ts (Frontend Faza 7)
========================================================================

  → Custom hook koji menja document.title (tab naslov u browseru)
  → Format: "Pregled — FinTracker", "Transakcije — FinTracker", itd.
  → Dodat na sve stranice: Dashboard, Transakcije, Kategorije, Budzeti

========================================================================
63. frontend/src/components/transactions/TransactionTable.tsx — Azuriran
========================================================================

  → Dodat responsive prikaz:
    - Desktop (md+): tabela sa svim kolonama
    - Mobilni (< md): kartice umesto tabele
  → Mobilna kartica prikazuje:
    - Kategorija + iznos u prvom redu
    - Datum, opis, i akcije u drugom redu
  → Dugmad za edit/delete su dostupna i na mobilnom

========================================================================
64. frontend/src/components/dashboard/CategoryPieChart.tsx — Azuriran
========================================================================

  → Toggle Rashodi/Prihodi sad RADI:
    - Rashodi: koriste podatke iz inicijalnog ucitavanja (bez novog API poziva)
    - Prihodi: poziva GET /api/dashboard/by-category?type=income
  → Spinner dok se podaci ucitavaju pri prebacivanju
  → Ukupan iznos se menja zavisno od tipa

========================================================================
========================================================================
  FRONTEND — FAZA 8: Lokalizacija (srpski → engleski)
========================================================================
========================================================================

========================================================================
65. Prevod svih korisničkih stringova na engleski (Frontend Faza 8)
========================================================================

  → Kompletna aplikacija prevedena sa srpskog na engleski jezik
  → NIJE uključen deploy (bez Docker frontend konfiguracije)
  → Izmenjeni fajlovi:

  frontend/index.html:
    → lang="sr" → lang="en"

  frontend/src/lib/utils.ts:
    → Locale promenjen sa "sr-Latn-RS" na "en-US" za formatCurrency
    → date-fns locale: sr → enUS
    → Datum format: "dd. MMM yyyy." → "MMM dd, yyyy" (engleski stil)

  frontend/src/layouts/DashboardLayout.tsx:
    → Navigacija: Pregled→Dashboard, Transakcije→Transactions,
      Kategorije→Categories, Budžeti→Budgets
    → "Odjavi se" → "Log out"

  frontend/src/pages/LoginPage.tsx:
    → Svi stringovi: Prijava→Sign in, Lozinka→Password, itd.

  frontend/src/pages/RegisterPage.tsx:
    → Svi stringovi: Registracija→Sign up, Kreiraj nalog→Create account, itd.

  frontend/src/pages/DashboardPage.tsx:
    → usePageTitle("Pregled") → usePageTitle("Dashboard")

  frontend/src/components/dashboard/SummaryCards.tsx:
    → Ukupni prihodi→Total Income, Ukupni rashodi→Total Expenses,
      Bilans→Balance, Broj transakcija→Transactions

  frontend/src/components/dashboard/MonthlyChart.tsx:
    → Mesečni trend→Monthly Trend, skraćenice meseci na engleskom

  frontend/src/components/dashboard/CategoryPieChart.tsx:
    → Po kategorijama→By Category, Rashodi/Prihodi→Expenses/Income

  frontend/src/components/dashboard/RecentTransactions.tsx:
    → Poslednje transakcije→Recent Transactions

  frontend/src/pages/TransactionsPage.tsx:
    → Sve poruke i labele na engleskom

  frontend/src/components/transactions/TransactionFilters.tsx:
    → Tip→Type, Svi→All, Prihodi→Income, Rashodi→Expense,
      Kategorija→Category, Od→From, Do→To, Resetuj→Reset

  frontend/src/components/transactions/TransactionForm.tsx:
    → Nova transakcija→New Transaction, Izmeni→Edit Transaction,
      Iznos→Amount, Kategorija→Category, Datum→Date, Opis→Description,
      Otkaži→Cancel, Sačuvaj→Save, Dodaj→Add

  frontend/src/components/transactions/DeleteConfirmModal.tsx:
    → Obriši transakciju→Delete Transaction, sve poruke na engleskom

  frontend/src/components/transactions/TransactionTable.tsx:
    → Zaglavlja tabele: Datum→Date, Kategorija→Category, Opis→Description,
      Tip→Type, Iznos→Amount, Prihod→Income, Rashod→Expense

  frontend/src/pages/CategoriesPage.tsx:
    → Kategorije→Categories, Nova kategorija→New Category,
      Prihodi→Income, Rashodi→Expenses, sve toast poruke na engleskom

  frontend/src/components/categories/CategoryForm.tsx:
    → Izmeni kategoriju→Edit Category, Naziv→Name, Tip→Type,
      Ikona→Icon, sve validacione poruke na engleskom

  frontend/src/components/categories/CategoryCard.tsx:
    → Prihod→Income, Rashod→Expense, Izmeni→Edit, Obriši→Delete

  frontend/src/pages/BudgetsPage.tsx:
    → Budžeti→Budgets, Novi budžet→New Budget, nazivi meseci na engleskom,
      Ukupno→Total, Preostalo→Remaining, Prekoračenje→Over budget

  frontend/src/components/budgets/BudgetForm.tsx:
    → Izmeni budžet→Edit Budget, Kategorija→Category,
      Planirani iznos→Planned Amount, sve validacije na engleskom

  frontend/src/components/budgets/BudgetCard.tsx:
    → Potrošeno→Spent, Preostalo→Remaining, Prekoračenje→Over budget

  frontend/src/components/ErrorBoundary.tsx:
    → Nešto je pošlo naopako→Something went wrong,
      Osveži stranicu→Refresh page

  frontend/src/hooks/useDashboard.ts:
    → Greška pri učitavanju→Failed to load data

  frontend/src/context/AuthContext.tsx:
    → Error poruka na engleskom

========================================================================
========================================================================
  FRONTEND — FAZA 9: Testovi, Docker, CI/CD
========================================================================
========================================================================

========================================================================
66. Frontend Unit Testovi — Vitest + React Testing Library
========================================================================

  → Alat: Vitest (brži od Jest-a, radi direktno sa Vite-om)
  → @testing-library/react — testira komponente iz perspektive korisnika
  → @testing-library/jest-dom — matcher-i za DOM (toBeVisible, toHaveClass, itd.)
  → jsdom — simulira browser environment u Node.js-u

  Instalirani paketi (devDependencies):
    vitest, @testing-library/react, @testing-library/jest-dom,
    @testing-library/user-event, jsdom

  frontend/vitest.config.ts:
    → Poseban config za testove (odvojen od vite.config.ts)
    → environment: "jsdom" — simulira browser
    → globals: true — describe/it/expect dostupni bez importa
    → setupFiles: ["./src/tests/setup.ts"] — globalni setup
    → css: false — ne procesira CSS u testovima (brže)

  frontend/src/tests/setup.ts:
    → Import @testing-library/jest-dom/vitest za custom matchere

  frontend/src/tests/utils.test.ts: (8 testova)
    → formatCurrency: formatira iznos sa RSD valutom
    → formatDate: konvertuje ISO string u čitljiv datum
    → cn: spaja CSS klase i uklanja duplikate (clsx + twMerge)

  frontend/src/tests/Button.test.tsx: (7 testova)
    → Renderuje tekst, onClick handler, disabled stanje,
      loading spinner, varijante klasa

  frontend/src/tests/Input.test.tsx: (5 testova)
    → Label, placeholder, error poruka, crvena bordura, user input

  frontend/src/tests/Modal.test.tsx: (4 testova)
    → Sakriven kad zatvoren, prikazan kad otvoren,
      onClose callback, blokira scroll

  frontend/src/tests/ErrorBoundary.test.tsx: (2 testa)
    → Renderuje decu, prikazuje error UI kad child baci grešku

  Komande:
    npm test          → pokreni sve unit testove (jednom)
    npm run test:watch → pokreni u watch modu (testira promene)
    npm run typecheck  → proveri TypeScript tipove

  UKUPNO: 26 unit testova — svi prolaze ✓

========================================================================
67. Frontend Dockerfile — Multi-stage build sa Nginx
========================================================================

  → Multi-stage build: dva koraka u jednom Dockerfile-u
  → Stage 1 (node:22-alpine): instalira zavisnosti + build-uje React app
  → Stage 2 (nginx:alpine): kopira gotov build + servira preko Nginx-a
  → Rezultat: mali Docker image (~30MB umesto ~800MB sa node_modules)

  frontend/Dockerfile:
    Stage 1 — Build:
      FROM node:22-alpine AS build
      → npm ci (instaliraj zavisnosti — brže od npm install)
      → npm run build (kreira dist/ folder sa optimizovanim fajlovima)

    Stage 2 — Serve:
      FROM nginx:alpine
      → Kopira nginx.conf konfiguraciju
      → Kopira dist/ folder iz Stage 1 u nginx html folder
      → Pokreće Nginx na portu 80

  frontend/nginx.conf:
    → SPA routing: try_files $uri $uri/ /index.html
      (sve rute idu na index.html — React Router handluje routing)
    → API proxy: /api/ → http://app:8000
      (Nginx prosleđuje API pozive ka backend servisu u Dockeru)
    → Cache za statičke fajlove: 1 godina (JS, CSS, slike, fontovi)

  frontend/.dockerignore:
    → Isključuje: node_modules, dist, .env, logove
    → Smanjuje Docker build context (brži build)

========================================================================
68. CI/CD — GitHub Actions za Frontend
========================================================================

  .github/workflows/ci.yml:
    → Dodat novi job: frontend-check
    → Pokreće se na svaki push/PR ka main grani

  frontend-check job:
    → Setup Node.js 22 sa npm cache-om
    → npm ci — instalacija zavisnosti
    → npm run lint — ESLint provera koda
    → npm run typecheck — TypeScript provera tipova
    → npm test — Vitest unit testovi (26 testova)
    → npm run build — provera da se projekat uspešno build-uje

  docker job (ažuriran):
    → needs: [test, lint, frontend-check] — čeka sve jobove
    → docker build -t fintracker-frontend ./frontend
    → Build-uje i backend i frontend Docker slike

========================================================================
69. E2E Testovi — Playwright
========================================================================

  → Playwright: end-to-end test framework od Microsoft-a
  → Testira aplikaciju u pravom browser-u (Chromium)
  → Pokreće Vite dev server automatski pre testova

  Instalirani paketi:
    @playwright/test — test runner + assertions
    Chromium browser — instaliran sa npx playwright install chromium

  frontend/playwright.config.ts:
    → testDir: "./e2e" — folder sa E2E testovima
    → baseURL: http://localhost:3000 — Vite dev server
    → webServer: automatski pokreće npm run dev pre testova
    → headless: true — bez vidljivog browser prozora
    → screenshot: "only-on-failure" — pravi screenshot samo kad test padne

  frontend/e2e/auth.spec.ts: (5 testova)
    → Prikazuje login stranicu po defaultu
    → Link ka register stranici radi
    → Register stranica ima sva polja forme
    → Validacija na prazan login submit
    → Navigacija između login i register

  frontend/e2e/navigation.spec.ts: (5 testova)
    → Neautentifikovani korisnik se redirectuje na /login za: /, /transactions, /categories, /budgets
    → Login stranica ima ispravan title (FinTracker)

  Komande:
    npm run test:e2e     → pokreni E2E testove (headless)
    npm run test:e2e:ui  → pokreni sa Playwright UI (za debugging)

  UKUPNO: 10 E2E testova — svi prolaze ✓

========================================================================
70. Docker Compose — Frontend servis
========================================================================

  backend/docker-compose.yml (ažuriran):
    → Dodat novi servis: frontend
    → build: ../frontend (koristi frontend/Dockerfile)
    → ports: 80:80 (Nginx servira na portu 80)
    → depends_on: app (čeka da backend bude spreman)
    → Nginx prosleđuje /api/ pozive ka app:8000

  Pokretanje cele aplikacije u Dockeru:
    cd backend && docker compose up --build
    → PostgreSQL na portu 5432
    → Redis na portu 6379
    → Backend (FastAPI) na portu 8000
    → Frontend (Nginx) na portu 80
    → Otvori http://localhost u browseru

========================================================================
POKRETANJE CELE APLIKACIJE (Backend + Frontend):
========================================================================

  Sve komande se pokreću iz ROOT foldera (FinTrackerApp/).
  Definisane su u package.json na root nivou.

  KORAK 1 — Pokreni Docker Desktop:
    → Otvori Docker Desktop i sačekaj da se pokrene (zelena ikonica)

  KORAK 2 — Pokreni Docker kontejnere (PostgreSQL + Redis):
    npm run docker

  KORAK 3 — Pokreni Backend (Terminal 1):
    npm run backend
    → Backend radi na http://localhost:8000
    → Swagger UI: http://localhost:8000/docs

  KORAK 4 — Pokreni Frontend (Terminal 2 — novi terminal!):
    npm run frontend
    → Frontend radi na http://localhost:3000

  OSTALE KOMANDE:
    npm run seed                  → ubaci test podatke u bazu (samo prvi put)
    npm run docker:down           → zaustavi PostgreSQL + Redis kontejnere

  NAPOMENA: Trebaš DVA terminala otvorena istovremeno!
    - Terminal 1: npm run backend  (port 8000)
    - Terminal 2: npm run frontend (port 3000)

========================================================================
ZAUSTAVLJANJE CELE APLIKACIJE:
========================================================================

  Terminal 2 (frontend):
    → Ctrl + C                    → zaustavi frontend dev server

  Terminal 1 (backend):
    → Ctrl + C                    → zaustavi backend server
    → npm run docker:down         → zaustavi PostgreSQL + Redis

========================================================================
ČIST RESTART (briše sve podatke iz baze):
========================================================================

  Terminal 1 (backend):
    → Ctrl + C
    → docker compose -f backend/docker-compose.yml down -v
    → npm run docker
    → npm run seed
    → npm run backend

  Terminal 2 (frontend):
    → npm run frontend
